A web app for comparing prices. This repo contains both the **server** and **client** code.  
How to run app locally: (Heroku app no longer runs)

git clone https://github.com/MadCat67/PriceCompareApp.git  
cd PriceCompareApp  
npm install  
npm run prod  
